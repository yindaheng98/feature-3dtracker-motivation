# Decision Index

| ID | Status | Decision | Evidence or reason | Detail |
| --- | --- | --- | --- | --- |
| DEC-HARNESS-001 | active | Store raw logs/artifacts in `output/harness-runs/`; keep only compact summaries and relative paths in durable memory. | Prevent large generated content from polluting model context. | this index |
| DEC-HARNESS-002 | active | Only the main agent writes canonical records and indexes; subagents return summaries or unique inbox candidates. | Prevent concurrent memory corruption and duplicate claims. | this index |
| DEC-HARNESS-003 | active | If an attempt misses the user's acceptance criteria, restore all attempt-owned code/configuration changes and verify every affected Git repository against its baseline before exit. Leave `rollback_pending` and block later attempts when safe restoration cannot be verified. | Prevent future work from silently building on a failed or partial implementation while preserving pre-existing user changes. | [root instructions](../../../AGENTS.md) |
| DEC-HARNESS-004 | active | Use the root `.venv` as the sole Python environment. Install or remove packages only with explicit `.venv/bin/python -m pip` commands and the protected-stack constraints; never hand-edit environment files or create per-submodule environments. | The projects' legacy pins conflict, while one resolver-checked shared set works without changing the selected Torch/CUDA stack. | [dependency manifests](../../dependencies/shared-requirements.txt) |
| DEC-HARNESS-005 | active | Build TrackerSplat from its repository directory with the upstream-documented `pip install --target . --upgrade --no-deps .` layout, using the root `.venv` interpreter and protected-stack constraint. | The user explicitly selected TrackerSplat's documented local-target installation mode. | [TrackerSplat README](../../../TrackerSplat/README.md) |
| DEC-HARNESS-006 | active | Classify environment failures by ownership: Agent may repair authorized Python-package/wheel issues in the sole `.venv`; user/host owns durable system libraries, executables, compiler/CUDA/driver and container-ABI changes, credentials, datasets, and checkpoints. | Prevent ineffective pip substitutes and ephemeral container fixes while letting the Agent autonomously solve reproducible Python-layer problems. | [environment guide](../../dependencies/native-prerequisites.md) |
| DEC-HARNESS-008 | active | Use root `checkpoints/` as the single model root for all five projects; retain flat TrackerSplat files and named subdirectories/caches for the others. | The user selected one checkpoint root; SpaTrackerV2 can use it through `HF_HOME`. | [decision card](DEC-HARNESS-008.md) |
| DEC-HARNESS-009 | active | Delete fully abandoned Harness content and every reference when no surviving code, artifact, result, or active decision requires its history. | Keep canonical memory focused on the current project rather than obsolete alternatives. | this index |

Create a separate decision card from `harness/templates/DECISION.md` when a
decision needs alternatives, consequences, or supersession history.
